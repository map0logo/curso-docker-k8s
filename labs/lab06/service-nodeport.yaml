apiVersion: v1
kind: Service
metadata:
  name: fastapi-nodeport-service
spec:
  type: NodePort
  selector:
    app: fastapi-app
  ports:
  - port: 80
    targetPort: 8000
    nodePort: 30007  # Rango: 30000-32767
    protocol: TCP

