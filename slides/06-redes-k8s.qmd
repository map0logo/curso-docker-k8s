---
title: Exponiendo aplicaciones con Kubernetes
subtitle: Redes en Kubernetes
date: last-modified
author:
  - name: Francisco Palm
    orcid: 0000-0002-1293-0868
    email: fpalm@qu4nt.com
    affiliations: qu4nt, activistasxsl
format:
  clean-revealjs
html-math-method:
  method: mathjax
  url: "https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"
lang: es
logo: images/qu4nt-logo.png
---

# Exponiendo aplicaciones con Kubernetes y Redes en Kubernetes

---

## De Docker a Kubernetes: El siguiente nivel

**¿Recuerdas Docker Compose?**

- Perfecto para single-host
- Desarrollo y testing
- Limitado para producción

**Problemas en escala:**

- ¿Y si un host falla?
- ¿Cómo escalo automáticamente?
- ¿Cómo gestiono updates sin downtime?
- ¿Y el load balancing?

## De Docker a Kubernetes: El siguiente nivel 2

```{mermaid}
graph TB
    A[Host Único] --> B[Single Point of Failure]
    C[Escala Manual] --> D[Ineficiente]
    E[Updates] --> F[Downtime]
    
    style B fill:#ffebee
    style D fill:#ffebee
    style F fill:#ffebee
```


**Kubernetes: La orquestación de contenedores a escala enterprise**

> En esta sesión aprenderemos a **exponer aplicaciones** del clúster y **gestionar su configuración** de forma segura y desacoplada.

## De Docker a Kubernetes: El siguiente nivel 3

Imagina esto:

- Tu app está corriendo feliz en Kubernetes…  
- Pero **nadie puede acceder a ella** desde fuera.  
- Y además, **sus credenciales están hardcodeadas** 🔥.

> 😱 ¡Dos grandes no-nos en producción!

Hoy resolveremos ambos problemas:  
✅ **Exposición controlada**  
✅ **Configuración limpia y segura**

---

## ¿Qué es Kubernetes realmente?

**Definición formal:**

- Sistema open-source para automatizar el despliegue, escalado y gestión de aplicaciones en contenedores

**En términos simples:**

- Como un "sistema operativo para clusters"
- Gestiona dónde se ejecutan tus contenedores
- Se asegura de que siempre estén corriendo
- Escala automáticamente según la demanda

## ¿Qué es Kubernetes realmente? 2

**Analogía:**

- Kubernetes es el director de orquesta
- Los nodos son los músicos
- Los pods son las partituras individuales

```{mermaid}
graph TB
    A[Kubernetes Master] --> B[Node 1]
    A --> C[Node 2]
    A --> D[Node 3]
    
    B --> E[Pods]
    C --> F[Pods]
    D --> G[Pods]
    
    style A fill:#ffccbc
```

---

## Arquitectura de Kubernetes: Visión general

```{mermaid}
graph TB
    A[Control Plane] --> B[API Server]
    A --> C[etcd]
    A --> D[Scheduler]
    A --> E[Controller Manager]
    
    F[Worker Nodes] --> G[Kubelet]
    F --> H[Kube Proxy]
    F --> I[Container Runtime]
    
    B --> G
    D --> J[Pods en Nodes]
    
    style A fill:#e3f2fd
    style F fill:#e8f5e8
```

---

## Conceptos fundamentales de Kubernetes

```{mermaid}
graph LR
    A[Kubernetes Concepts] --> B[Pods]
    A --> C[Services]
    A --> D[Deployments]
    A --> E[Ingress]
    A --> F[Namespaces]
    
    B --> G[Unidad mínima desplegable]
    C --> H[Punto de acceso estable]
    D --> I[Gestión de aplicaciones]
    E --> J[Acceso desde exterior]
    F --> K[Aislamiento lógico]
```

---

## Pods: La unidad atómica de Kubernetes

**¿Qué es un Pod?**

- La unidad más pequeña que puedes desplegar
- Grupo de uno o más contenedores
- Comparten recursos de red y almacenamiento
- Mismo contexto de ejecución

**Características clave:**

- Dirección IP única compartida
- Namespace de red compartido
- Volúmenes compartidos
- Ciclo de vida conjunto

## Pods: La unidad atómica de Kubernetes 2

```{mermaid}
graph TB
    A[Pod IP: 10.0.0.5] --> B[Contenedor App]
    A --> C[Contenedor Sidecar]
    A --> D[Volumen compartido]
    
    B --> E[Puerto 8080]
    C --> F[Puerto 9090]
    
    style A fill:#e1f5fe
```

**💡 Importante: Normalmente 1 contenedor por Pod, pero pueden ser más para casos especiales**

---

### ¿Cómo hablan las apps con el mundo?

En Kubernetes, los **pods son efímeros**: nacen, mueren, se reemplazan.  
Sus IPs **cambian constantemente** → ¡No puedes depender de ellas!

> 🧠 **Necesitas una identidad estable** para tu aplicación.  
> Eso es exactamente lo que ofrece un **Service**.

---

## Services: La red estable en Kubernetes

**Problema:**

- Los Pods son efímeros (nacen y mueren)
- Las IPs de los Pods cambian constantemente
- ¿Cómo encuentran los clientes a los Pods?

**Solución: Services**

- Punto de acceso estable y permanente
- IP virtual que no cambia
- Balancea carga entre Pods backend
- Descubre automáticamente los Pods

## Services: La red estable en Kubernetes 2

```{mermaid}
graph LR
    A[Cliente] --> B[Service IP: 10.3.241.150]
    B --> C[Pod A: 10.0.0.5]
    B --> D[Pod B: 10.0.0.6]
    B --> E[Pod C: 10.0.0.7]
    
    style B fill:#c8e6c9
```

Un **Servicio** es una abstracción que define un **punto de acceso estable** a un conjunto de pods.

- Usa **etiquetas (labels)** para seleccionar pods.
- Proporciona una **IP virtual** y un **nombre DNS** fijos.
- Actúa como **balanceador de carga interno**.

> 💡 **El Service no se preocupa por IPs reales** → solo por etiquetas.  
> ¡Los pods pueden morir y renacer, y el Service siempre los encontrará!

## Tipos de Servicios

```{mermaid}
graph LR
    A[Service Types] --> B[ClusterIP]
    A --> C[NodePort]
    A --> D[LoadBalancer]
    A --> E[ExternalName]
    
    B --> F[Interno al cluster]
    C --> G[Acceso mediante
    puertos del nodo]
    D --> H[Cloud Load
    Balancer externo]
    E --> I[Alias para
    servicio externo]
    
    style B fill:#e1f5fe
    style C fill:#fff3e0
    style D fill:#f3e5f5
```

---

## ClusterIP: Comunicación interna (por defecto)

**Características:**

- IP accesible solo dentro del cluster
- Default type para servicios
- Perfecto para comunicación entre microservicios

**Ejemplo típico:**

- Frontend → Backend API
- API → Database
- Servicios internos que no exponen al exterior


## ClusterIP: Comunicación interna (por defecto) 2

```{mermaid}
graph TB
    A[Frontend Pod] --> B[API Service<br/>clusterip: 10.96.1.5]
    B --> C[API Pod 1]
    B --> D[API Pod 2]
    
    style B fill:#e1f5fe
```


## NodePort: Acceso externo básico

**Características:**

- Expone el servicio en un puerto estático en cada nodo
- Accesible desde fuera del cluster
- Puerto en rango 30000-32767

**Flujo de tráfico:**

1. Cliente → NodeIP:NodePort
2. Nodo → Service
3. Service → Pod destino

## NodePort: Acceso externo básico 2

```{mermaid}
graph TB
    A[Cliente] --> B[NodeIP:30080]
    B --> C[Service]
    C --> D[Pod Backend]
    
    style B fill:#fff3e0
```


**⚠️ No ideal para producción - usa Ingress**

---

## LoadBalancer: Integración con cloud

**Características:**

- Crea un load balancer externo en cloud providers
- Asigna IP externa automáticamente
- Integración nativa con AWS, GCP, Azure

**Ideal para:**

- Servicios que necesitan IP pública estable
- Aplicaciones críticas en cloud
- Cuando necesitas balanceo de carga gestionado


## LoadBalancer: Integración con cloud 2

```{mermaid}
graph TB
    A[Internet] --> B[Cloud LB: 35.200.10.5]
    B --> C[Node 1]
    B --> D[Node 2]
    B --> E[Node 3]
    
    C --> F[Service]
    D --> F
    E --> F
    
    style B fill:#f3e5f5
```

---

> 🧩 **Regla mental**:  

> - Interno → `ClusterIP`  
> - Pruebas → `NodePort`  
> - Producción → `LoadBalancer`

---

## ¿Cómo se define un Service?

```yaml
apiVersion: v1
kind: Service
meta
  name: mi-app
spec:
  type: LoadBalancer        # ← ¡elige el tipo!
  selector:
    app: fastapi            # ← debe coincidir con los labels de tus pods
  ports:
    - protocol: TCP
      port: 80              # puerto del Service
      targetPort: 8000      # puerto del contenedor
```

> 🔍 **El secreto está en el `selector`**:  
> Si tus pods tienen `app: fastapi`, ¡el Service los encontrará!

---

## Ingress: El router inteligente

**Problema con LoadBalancer:**

- Un LoadBalancer por servicio → costoso
- No puede hacer routing basado en paths/hosts

**Solución: Ingress**

- Un punto de entrada para múltiples servicios
- Routing basado en reglas (host, path, headers)
- Terminación SSL/TLS
- Rewrite rules y autenticación


## Ingress: El router inteligente 2

```{mermaid}
graph TB
    A[Internet] --> B[Ingress Controller]
    B --> C["/api → API Service"]
    B --> D["/app → Web Service"]
    B --> E["/docs → Docs Service"]
    
    style B fill:#c8e6c9
```


## Ingress vs LoadBalancer

```{mermaid}
graph TB
    A[Tráfico Entrante] --> B{¿Necesitas routing
    avanzado?}
    
    B -->|Sí| C[Ingress Controller]
    B -->|No| D[LoadBalancer Service]
    
    C --> E[Routing por host/path]
    C --> F[SSL Termination]
    C --> G[Un LB para múltiples apps]
    
    D --> H[IP pública directa]
    D --> I[Simple y directo]
    
    style C fill:#c8e6c9
    style D fill:#f3e5f5
```

---

## Deployments: Gestionando el ciclo de vida


**¿Qué resuelve?**

- ¿Cómo actualizo mi app sin downtime?
- ¿Cómo escalo automáticamente?
- ¿Cómo vuelvo a una versión anterior?

**Deployment proporciona:**

- Updates rolling (sin downtime)
- Rollbacks automáticos
- Escalado automático (HPA)
- Replica management

## Deployments: Gestionando el ciclo de vida 2

```{mermaid}
graph TB
    A[Deployment] --> B[ReplicaSet v1]
    A --> C[ReplicaSet v2]
    
    B --> D[Pods v1.0]
    C --> E[Pods v1.1]
    
    style A fill:#ffccbc
```


---

## Modelo de red de Kubernetes

**Principios fundamentales:**

1. Todo Pod puede comunicarse con todo Pod sin NAT
2. Todo Node puede comunicarse con todo Pod sin NAT
3. La IP que un Pod ve en sí mismo es la misma que otros Pods ven

```{mermaid}
graph LR
    A[Pod A] --> B[Pod B]
    C[Pod C] --> B
    A --> C
    
    style A fill:#e1f5fe
    style B fill:#f5e1fe
    style C fill:#f5fee1
```


## Modelo de red de Kubernetes 2

**Implementación:**

- CNI (Container Network Interface) plugins
- Cada Pod tiene IP real de red
- No overlays complicados (en la mayoría de casos)


## DNS en Kubernetes

**Descubrimiento automático de servicios:**

- Cada Service obtiene un DNS name
- Formato: `<service-name>.<namespace>.svc.cluster.local`
- Resolución dentro del cluster automática

**Ejemplos:**

- `frontend.default.svc.cluster.local`
- `database.production.svc.cluster.local`
- `redis.cache.svc.cluster.local`


## DNS en Kubernetes

**Simplificado:**

- `frontend` (mismo namespace)
- `database.production` (namespace diferente)

```{mermaid}
graph LR
    A[App Pod] --> B[CoreDNS]
    B --> C[Resuelve: api-service]
    C --> D[10.96.1.5]
    
    style B fill:#fff9c4
```


---

## Namespaces: Aislamiento lógico

**¿Para qué sirven?**

- Aislar recursos (dev, staging, prod)
- Límites de recursos por equipo
- Control de acceso (RBAC)

**Namespaces comunes:**

- `default` (por defecto)
- `kube-system` (sistema de Kubernetes)
- `kube-public` (recursos públicos)
- `custom` (tus aplicaciones)


## Namespaces: Aislamiento lógico 2

```{mermaid}
graph TB
    A[Cluster] --> B[Namespace: dev]
    A --> C[Namespace: staging]
    A --> D[Namespace: prod]
    
    B --> E[Pods/Services dev]
    C --> F[Pods/Services staging]
    D --> G[Pods/Services prod]
    
    style B fill:#e1f5fe
    style C fill:#fff3e0
    style D fill:#c8e6c9
```

---

## Flujo completo: De imagen a servicio expuesto

```{mermaid}
graph TB
    A[Imagen Docker] --> B[Deployment]
    B --> C[Pods]
    C --> D[Service]
    D --> E[Ingress]
    E --> F[Internet]
    
    G[ConfigMap] --> C
    H[Secret] --> C
    I[PersistentVolume] --> C
    
    style D fill:#e1f5fe
    style E fill:#c8e6c9
```

---

## ConfigMaps y Secrets: Configuración

**ConfigMaps:**

- Configuración no sensible
- Variables de entorno, archivos de config
- Puede montarse como volumen

**Secrets:**

- Información sensible
- Passwords, tokens, keys
- Encriptados en etcd


## ConfigMaps y Secrets: Configuración 2

**Uso en Pods:**

- Variables de entorno
- Volúmenes montados
- Image pull secrets

Un **ConfigMap** es un objeto de Kubernetes que almacena **datos de configuración no sensibles** como:

- URLs de servicios
- Nombres de bases de datos
- Flags de feature
- Archivos de configuración (`.env`, `.yaml`, etc.)

## ConfigMaps y Secrets: Configuración 3

> 📦 Piensa en un ConfigMap como un **diccionario clave-valor** que vive en el clúster.

```{mermaid}
graph TB
    A[ConfigMap] --> B[Pod]
    C[Secret] --> B
    
    style C fill:#ffebee
```

```{mermaid}
graph LR
  ConfigMap["ConfigMap: db-config"] -->|injected as| Pod["Pod: fastapi"]
  Pod --> App["App lee: DB_URL"]
```

---

## Secrets: Tu bóveda de seguridad

### ¿Qué es un Secret?

Un **Secret** es como un ConfigMap, pero para **datos sensibles**:

- Contraseñas
- Tokens de API
- Claves SSH
- Certificados TLS

## Secrets: Tu bóveda de seguridad 2

> 🔒 Los Secrets se almacenan **codificados en base64** (¡pero no cifrados por defecto!).  
> En producción, usa **encriptación en reposo** o integración con **Vault/Cloud KMS**.

> 💡 **Regla de oro**:  
> Si no quieres que aparezca en un screenshot… ¡es un Secret!

## ¿Cómo inyectar ConfigMaps y Secrets en tus Pods?

Tienes **dos formas principales**:

### 1. Como **variables de entorno**

```yaml
env:
  - name: DB_URL
    valueFrom:
      configMapKeyRef:
        name: db-config
        key: url
  - name: DB_PASSWORD
    valueFrom:
      secretKeyRef:
        name: db-credentials
        key: password
```

### 2. Como **archivos en un volumen**

```yaml
volumeMounts:
  - name: config-volume
    mountPath: /etc/config
volumes:
  - name: config-volume
    configMap:
      name: app-config
```

> 🧠 **¿Cuándo usar cada uno?**  
> - Variables de entorno → para pocos valores, simples.  
> - Archivos → para configuraciones complejas (ej: `nginx.conf`, `appsettings.json`).

---

## Almacenamiento persistente en Kubernetes

**Problema:**

- Los Pods son efímeros
- El almacenamiento del Pod se pierde al reiniciar
- ¿Dónde guardo bases de datos, archivos?

**Solución: Persistent Volumes**

- Almacenamiento persistente independiente del Pod
- Diferentes backends (local, cloud, NFS, etc.)
- Claim mechanism (PVC) para solicitar almacenamiento


## Almacenamiento persistente en Kubernetes 2

```{mermaid}
graph TB
    A[Pod] --> B[PersistentVolumeClaim]
    B --> C[PersistentVolume]
    C --> D[Almacenamiento físico]
    
    style C fill:#fff3e0
```

## Patrón típico: Aplicación web completa

```{mermaid}
graph TB
    A[Usuario] --> B[Ingress]
    B --> C[Frontend Service]
    B --> D[API Service]
    
    C --> E[Frontend Pods]
    D --> F[API Pods]
    F --> G[Database Service]
    G --> H[Database Pod]
    
    I[ConfigMap] --> E
    I --> F
    J[Secret] --> F
    J --> H
    K[Persistent Volume] --> H
    
    style B fill:#c8e6c9
    style C fill:#e1f5fe
    style D fill:#e1f5fe
    style G fill:#e1f5fe
```

---

## Buenas prácticas de seguridad

### Con Secrets:

1. **Nunca hagas commits de secretos al repositorio** 

   → Usa `.gitignore` y herramientas como `sealed-secrets` o `SOPS`.

2. **Limita el acceso con RBAC** 

   → Solo los pods que lo necesitan deben poder leer secretos.

3. **Rota las credenciales regularmente** 

   → Kubernetes actualiza los Secrets en los pods **automáticamente** (¡pero la app debe recargarlos!).

> ⚠️ **¡Ojo!** Los secretos montados como archivos **se actualizan en vivo**,  
> pero las variables de entorno **NO** (requieren reinicio del pod).

---

## Resumen visual: Arquitectura ideal

```{mermaid}
graph LR
  Internet -->|HTTP| Service[Service: LoadBalancer]
  Service --> Deployment[Deployment: fastapi]
  Deployment --> Pod1[Pod]
  Deployment --> Pod2[Pod]
  Pod1 -->|lee| ConfigMap[ConfigMap: db-url]
  Pod1 -->|lee| Secret[Secret: db-pass]
  Pod2 -->|lee| ConfigMap
  Pod2 -->|lee| Secret
```

- **Exposición**: `LoadBalancer` → tráfico externo  
- **Configuración**: `ConfigMap` + `Secret` → inyectados en pods  
- **Aplicación**: código limpio, sin secretos, sin URLs hardcodeadas

---

## ¿Qué NO hacer?

| ❌ Mal | ✅ Bien |
|--------|--------|
| `password = "m1s3cr3t"` en el código | Usar un Secret |
| Exponer con `NodePort` en producción | Usar `LoadBalancer` + Ingress |
| Hardcodear `localhost:5432` | Usar ConfigMap con `db.prod.svc.cluster.local` |
| Reiniciar pods manualmente al cambiar config | Diseñar apps que recarguen config en caliente |

> 🚫 **La configuración quemada = deuda técnica con intereses**.


## Escalado automático: HPA

**Horizontal Pod Autoscaler:**

- Escala automáticamente el número de Pods
- Basado en métricas de CPU, memoria o custom
- Define mínimos y máximos

**Ejemplo:**

- CPU > 70% → añadir Pods
- CPU < 30% → quitar Pods
- Mínimo: 2 Pods, Máximo: 10 Pods


## Escalado automático: HPA 2

```{mermaid}
graph LR
    A[Métricas] --> B[HPA]
    B --> C[Deployment]
    C --> D["\+ Pods"]
    C --> E["\- Pods"]
    
    style B fill:#f3e5f5
```

## Service Mesh: La evolución de las redes

**¿Qué resuelve?**

- Observabilidad (metrics, traces, logs)
- Comunicación segura (mTLS)
- Control de tráfico avanzado
- Resiliencia (retries, timeouts, circuit breakers)

**Implementaciones populares:**

- Istio
- Linkerd
- Consul Connect


## Service Mesh: La evolución de las redes 2

```{mermaid}
graph TB
    A[Service Mesh] --> B[Sidecar Proxy]
    A --> C[Control Plane]
    
    B --> D[Comunicación segura]
    B --> E[Métricas]
    B --> F[Control tráfico]
    
    style A fill:#e8f5e8
```

---

## Resumen: Exponiendo aplicaciones

| Recurso | Propósito | Cuándo usar |
|---------|-----------|-------------|
| **Pod** | Unidad de ejecución | Contenedores de tu app |
| **Service** | Punto de acceso estable | Comunicación interna/estable |
| **Ingress** | Routing HTTP/S | Acceso externo con reglas |
| **LoadBalancer** | IP pública directa | Servicios que necesitan IP única |

## Resumen: Redes en Kubernetes

| Concepto | Función | Importancia |
|----------|---------|-------------|
| **CNI** | Networking de pods | Comunicación pod-to-pod |
| **DNS** | Descubrimiento de servicios | Resolución automática de nombres |
| **Network Policies** | Seguridad de red | Aislamiento y control de tráfico |
| **Service Mesh** | Redes avanzadas | Observabilidad y control fino |

---

## Buenas prácticas de networking

**Seguridad:**

- Usa Network Policies para aislar tráfico
- Limita exposición de servicios
- Usa mTLS para comunicación interna sensible

**Rendimiento:**

- Elegir CNI plugin apropiado para tu caso
- Monitorizar métricas de red
- Optimizar tamaño de pods y recursos


## Buenas prácticas de networking 2

**Mantenibilidad:**

- Nomenclatura consistente para servicios
- Documentar dependencias entre servicios
- Usar labels y annotations efectivamente

---

## Evolución del networking en Kubernetes

```{mermaid}
graph LR
    A[Pods básicos] --> B[Services]
    B --> C[Ingress]
    C --> D[Network Policies]
    D --> E[Service Mesh]
    
    style B fill:#e1f5fe
    style C fill:#c8e6c9
    style E fill:#e8f5e8
```

**Desde lo simple hasta enterprise-grade**

---

## ¡Preparados para el mundo real!

**Lo que hemos cubierto:**

- Fundamentos de Kubernetes y su arquitectura
- Pods, Services y Deployments
- Estrategias para exponer aplicaciones
- Modelo de red y DNS de Kubernetes
- Conceptos avanzados como Service Mesh

**Siguiente paso:** Laboratorio práctico donde desplegaremos aplicaciones reales en Kubernetes y configuraremos redes complejas.